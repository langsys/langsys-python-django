# Conformance — `langsys-django`

| | |
|---|---|
| **SDK** | `langsys-django`, the Django binding over the `langsys` Python core |
| **Spec revision read** | langsys2 a95af2c2…, docs/sdk-spec.mdx blob 5d7e6890b733a50fb6f5f5c30e0056c6ef7bcf45 |
| **Profiles** | server, binding — derived: binding over langsys-python |
| **specVersion** | 8.2.18 |
| **Spec pin, re-derived** | `git -C ~/Documents/dev/langsys2 rev-parse a95af2c2596d5a882473d9ef09d232eb5c1d7a12:docs/sdk-spec.mdx` → `5d7e6890b733a50fb6f5f5c30e0056c6ef7bcf45`, re-derived when this file was written. It is pinned by commit, never by branch, and `test_the_pinned_rule_list_is_the_spec_blob_itself` re-derives the rule list on every run where langsys2 is present |
| **SDK revision** | branch `feature/838_write_key_gating` |
| **Core consumed** | `langsys-python` at `e834170` (`e834170e0830475f8d4640f33f0e5ac8ec31d0ca`), which has the request scopes, the SRV-6 resolver with the framework's locale, server messages with their attachment to a framework's body, and the template list's label placeholders and strict flag. The suite runs against a clean `git archive` of that commit placed first on `PYTHONPATH`; local development uses the core's working copy through a symlink excluded from git. Delegated rows are graded against the core's conformance file at that commit, which rows spec 8.2.18's 113 ids |
| **Suite** | 151 tests in 12 files, all passing, against the core's working copy and against the clean archive of `e834170`. `ruff` and `mypy --strict` are clean |
| **Runtime** | Django 4.2.30 and Django REST framework 3.16.1 on CPython 3.9.6 |

**Profile.** The spec's per-SDK profile table rows this package `server, binding` over `langsys-python`,
with terminal HTML. So it takes the server rules through its core, the binding rules itself, and the SRV
family for the render it performs inside a request. Every profile-based `n/a` below is checked against
its rule's own Profiles line by `test_profile_rows_agree_with_each_rules_profiles_line`.

**Evidence tiers follow CONF-2.** `live`, `contract` and `mock` apply only where the governed property
depends on what the API answers. Everything this binding owns is an in-process property: which locale
a request is served in and what its response says it varied on, whether its page is marked resolved,
when the core's scope, flush and reset run relative to Django's lifecycle, what a failed form or
serializer hands the core, and what the code does not contain. So every binding-owned row is
`n/a (pure)`. Every property that does depend on the API is the core's, and its row is `delegated`, with
tier `-` and the core's own grade named in the evidence.

## What surfaced while writing this

1. **The middleware decided capability itself (BIND-2, GATE-2).** It collapsed an unreachable authorize
   into "no" and discarded a queue the core would have held. *(Reviewer.)*
2. **The write decision survived the request (GATE-3).** A later request acted on an earlier request's
   decision, and the OBS-1 notice fired once per process. *(Reviewer.)*
3. **Registration ran on the request path (SRV-3, REG-3).** *(Reviewer.)*
4. **`AUTO_FLUSH` was a binding-only discovery switch (BIND-4).** *(Reviewer.)*
5. **A streamed response rendered in the base language, and its misses were never sent.**
6. **`{% t %}` turned an undefined template variable into `""` (BIND-1, ICU-2, ICU-3),** so the core
   rendered `Hi !` where it renders the visible gap `Hi {name}!`, and a plural leaked its raw ICU source.
7. **The core's debounce and process-wide queue could send a request's misses before its response was
   out (SRV-3).** The core's request scopes (`1229ca2`) close it, and the middleware opens one per request.
8. **Two of this binding's tests could not fail at first, and mutation showed it**: an SRV-2 test with one
   barrier, and a debounce test that raced a sleep against a timer.
9. **Probes can misfire in both directions.** One matched Django's `var.resolve(context)`, another Django's
   `django.utils.cache` import, and a control stopped firing when the core's live tree moved the code it
   pointed at. Probes read the package as imported, match the core's own forms, and take their controls
   from the modules that define what they probe.
10. **`t()` and `{% t %}` cannot pass a placeholder named `category` or `phrase` (BIND-1).** The core's
    `params` mapping can. Measured in the FastAPI lane first and confirmed here.
11. **No response said it varied by locale.** One URL served several languages with no `Vary`, which a
    shared cache turns into a cross-serve; the Rails lane reproduced it on a CDN. SRV-6 closes it here:
    where the app resolved no locale, the middleware resolves it through the core and adds the `Vary`
    headers the choice names; a locale Django resolved is Django's and the app's to vary on.
12. **A live core tree is not a pinned one.** A local run went red while the core's mutation harness was
    rewriting its source in place. The core now mutates an isolated copy, and this file's evidence is the
    run against a clean archive of the core.

## Summary

Counted from the status table by `test_the_summary_counts_are_the_tables_counts`. It fails if the table
and these numbers disagree.

| Status | Count | |
|---|---|---|
| `implemented` | 25 | GATE-3, GATE-10, REG-3, SRV-1, SRV-2, SRV-3, SRV-6, MSG-1, MSG-2, MSG-3, MSG-4, MSG-5, MSG-7, MSG-9, MSG-10, MSG-11, BIND-2, BIND-3, BIND-4, BIND-5, BIND-6, WIRE-5, CONF-1, CONF-2 and CONF-3 |
| `partial` | 1 | BIND-1 waits on the operator's choice of `t()` signature |
| `not implemented` | 1 | MIG-8 |
| `delegated` | 61 | core-owned behaviour, each with an absence probe and a firing control |
| `n/a (profile)` | 21 | browser-only rules |
| `n/a (architecture)` | 4 | HINT-13, SRV-4, SRV-5 and MSG-12 |

**Delegated rows whose core row is not green count red for this lane,** because the checker resolves each
one against the core's current grade. They stay `delegated` because the binding takes part in none of them.
- Not green in the core's file at `e834170`: MIG-9 (`not implemented`).
- Provisional there, on mock evidence: none.

## Status

| Rule | Status | Tier | Evidence |
|---|---|---|---|
| GATE-1 | delegated | - | core `GATE-1` (implemented, live). Probe `capability` matches nothing in this binding and fires on core `client.py` and on this binding's `middleware.py` at `34a6a87`, which branched on `can_write` |
| GATE-2 | delegated | - | core `GATE-2` (implemented, contract): the core holds the queue when capability is unknown. Probe `queue-mutation` matches nothing here and fires on core `client.py` and on `middleware.py` at `34a6a87`, whose `clear_pending()` discarded a held queue. End to end, `test_BIND2_capability_unknown_holds_the_queue_through_the_binding` was red at `34a6a87` (finding 1), and mutation M1 reddens it |
| GATE-3 | implemented | n/a (pure) | The core's declared wrapper obligation. On `request_finished` the binding calls the core's `reset_write_decision()` after the flush, so the flush uses the decision this request observed and the next request starts without it. `test_GATE3_an_observed_write_decision_does_not_survive_the_request` covers the latch, on the discriminating vector where `key_type` and `write_enabled` disagree. `test_GATE3_the_unusable_capability_notice_rearms_at_each_request` uses a legitimate `ip_write` answer. Both were red at `34a6a87`, and mutation M3 reddens both. No carve-out is taken |
| GATE-4 | delegated | - | core `GATE-4` (implemented, n/a (pure)). Probe `cache`: this binding writes no cache, so it has nothing to strip. It fires on core `catalog.py` |
| GATE-5 | delegated | - | core `GATE-5` (implemented, contract). Probe `queue-mutation`: this binding keeps no queue or bookkeeping. It fires on core `client.py` and on `middleware.py` at `34a6a87` |
| GATE-6 | delegated | - | core `GATE-6` (n/a (architecture: no report lane exists, per HINT-2; live if one is ever added), -). Probe `registration-request` shows no registration is constructed here and fires on core `registration.py`. Probe `report-lane` shows there is no report lane and fires on the TypeScript core's `api.ts` and on a synthetic call. Choosing the lane is the core's alone. |
| GATE-7 | delegated | - | core `GATE-7` (implemented, contract). Probe `catalog-read`: this binding never reads the catalog, so it cannot detect a miss the core does not see. It fires on core `client.py`. Every entry point this binding adds reaches the core: `test_GATE7_every_entry_point_reaches_the_core_queue` covers the tag, filter and helper, and `test_SRV3_a_streamed_body_is_complete_before_its_misses_are_sent` covers a streamed render. Mutation M7 reddens the first |
| GATE-8 | delegated | - | core `GATE-8` (implemented, contract). Probe `capability`: nothing here reads `write_enabled` or `key_type`, so there is no absence to misread. It fires on core `client.py` and on `middleware.py` at `34a6a87` |
| GATE-9 | n/a (profile: browser) | - | The project's base-locale discovery gate governs a browser client's miss path; this binding records misses only through the core |
| GATE-10 | implemented | n/a (pure) | Producing half: `{% langsys_resolved %}` goes on the element a layout owns, usually `<html>`, and prints a bare `data-ls-resolved` when the request renders in a locale other than the project's base, so a browser SDK on the page never takes the text `{% t %}` printed for source. A base-locale render, and one whose project locales cannot be read (the SDK then serves its configured base), stay unmarked and discoverable. `test_GATE10_a_render_in_a_non_base_locale_marks_its_root`, with `test_GATE10_a_base_locale_render_stays_unmarked` as its control, `test_GATE10_unreadable_project_locales_leave_the_page_unmarked` and `test_GATE10_outside_a_request_the_marker_prints_nothing`. Mutations M18 (a base-locale render marked) and M19 (nothing ever marked) each redden a named test. Reading half: this binding has no DOM host that reads the marker, because `{% t %}` and `t()` are bare lookups, outside the rule by construction. `translate_page()` produces its own marker in the core |
| CAT-1 | delegated | - | core `CAT-1` (implemented, n/a (pure)). Probe `catalog-read` matches nothing here and fires on core `client.py` |
| CAT-2 | delegated | - | core `CAT-2` (implemented, n/a (pure)). Probe `catalog-read` matches nothing here and fires on core `client.py` |
| CAT-3 | delegated | - | core `CAT-3` (implemented, n/a (pure)). Probe `block-identity`: there is no content-block handling here. It fires on core `client.py` |
| REG-1 | delegated | - | core `REG-1` (implemented, n/a (pure)). Probe `registration-request`: this binding constructs no registration. It fires on core `registration.py`. Through the handler, `test_SRV3_a_read_only_key_pushes_nothing` shows the request hook opens no send path around the core's gate |
| REG-2 | delegated | - | core `REG-2` (implemented, n/a (pure)). Probe `scheduling`: there is no timer, sleep or debounce here. It fires on core `client.py`. The core's debounce is left as the core configures it; inside a request, the core's request scope holds a miss until the response is out (SRV-3) |
| REG-3 | implemented | n/a (pure) | The core's declared wrapper obligation. The core's public `flush_pending()` runs on every `request_finished`, not only at process exit. `test_REG3_the_queue_is_registered_at_the_end_of_every_request` runs with no debounce, no exit hook and no explicit flush, so the request boundary is its only send path. Mutation M2 reddens it along with every other request-boundary test. The public manual flush and the best-effort exit hook remain the core's, and the README points work outside a request at `get_client().flush_pending()` |
| REG-4 | n/a (profile: browser) | - | Browser teardown via `keepalive`. A server has no page teardown |
| REG-5 | n/a (profile: browser) | - | The browser teardown flush path. A server has no page teardown |
| REG-6 | delegated | - | core `REG-6` (implemented, n/a (pure)). Probe `queue-mutation`: this binding never snapshots or clears the queue. It fires on core `client.py` and on `middleware.py` at `34a6a87`, which cleared it |
| REG-7 | delegated | - | core `REG-7` (implemented, n/a (pure)). Probe `send-concurrency` matches nothing here and fires on core `client.py`. The request hook calls the core's flush, which declines while a send is in flight |
| REG-8 | delegated | - | core `REG-8` (implemented, contract). Probe `scheduling`: there is no retry or backoff here. It fires on core `client.py` |
| REG-9 | delegated | - | core `REG-9` (implemented, contract). Probe `batching` matches nothing here and fires on core `registration.py` |
| REG-10 | delegated | - | core `REG-10` (implemented, live). Probe `failure-shape`: there is no swallowed exception and no success-shaped result here. It fires on core `client.py` and on `middleware.py` at `34a6a87`, which wrapped its flush in `except Exception` |
| REG-11 | delegated | - | core `REG-11` (implemented, n/a (pure)). Probe `ellipsis` matches nothing here and fires on core `client.py` |
| REG-12 | delegated | - | core `REG-12` (implemented, n/a (pure)). Probe `block-identity` matches nothing here and fires on core `client.py` |
| REG-13 | delegated | - | core `REG-13` (implemented, n/a (pure)). Probe `catalog-read` matches nothing here and fires on core `client.py`. The binding never reads the catalog, so it decides nothing about what is unregistered |
| HINT-1 | n/a (profile: browser) | - | The report payload. A server SDK never reports (HINT-2) |
| HINT-2 | delegated | - | core `HINT-2` (implemented, n/a (pure)). Probe `report-lane` matches nothing here. It fires on the TypeScript core's `api.ts`, which posts to `discovery/hint`, and on a synthetic call, so a report lane added to this binding would turn it red |
| HINT-3 | n/a (profile: browser) | - | URL capture timing for reports. A server has no report lane |
| HINT-4 | n/a (profile: browser) | - | Per-URL session dedup in `sessionStorage`, which is browser state |
| HINT-5 | n/a (profile: browser) | - | Report jitter. A server has no report lane |
| HINT-6 | n/a (profile: browser) | - | URL normalisation for reports. The backend mirror is not an SDK rule |
| HINT-7 | n/a (profile: browser) | - | Report-lane reliability. A server has no report lane |
| HINT-8 | n/a (profile: browser) | - | No reporting during server rendering. A server has no report lane |
| HINT-9 | n/a (profile: browser) | - | The `auto_discovery` policy for reports. A server has no report lane |
| HINT-10 | n/a (profile: browser) | - | Declining credential-bearing URLs in reports. A server has no report lane |
| HINT-11 | n/a (profile: browser) | - | Testing both fragment shapes in reports. A server has no report lane |
| HINT-12 | n/a (profile: browser) | - | Normalisation parity across report legs. The server mirror is backend behaviour |
| HINT-13 | n/a (architecture: every Django route is its own server request, so there is no client-side router and no persistent mounted layout to re-enter; live if this binding ever ships a client-side integration) | - | The navigation entry point belongs to a browser core. A route change here is a new request, which re-enters the SDK in full |
| ICU-1 | delegated | - | core `ICU-1` (implemented, n/a (pure)). Probe `interpolation`: there is no interpolation here. It fires on core `interpolate.py` |
| ICU-2 | delegated | - | core `ICU-2` (implemented, n/a (pure)). Probe `interpolation` fires on core `interpolate.py`. The template path is where a binding could narrow this rule: `{% t %}` now passes an undefined variable as missing. See `test_ICU2_an_undefined_template_variable_is_an_absent_argument` and `test_ICU2_a_context_value_of_none_is_an_absent_argument`, with `test_control_an_explicit_empty_string_is_still_a_value` keeping emptiness distinct from absence. At `34a6a87` the first rendered `Hi !`, and mutation M8 reddens it |
| ICU-3 | delegated | - | core `ICU-3` (implemented, n/a (pure)). Probe `interpolation` fires on core `interpolate.py`. At `34a6a87`, `test_ICU3_a_plural_over_an_undefined_variable_recovers_instead_of_leaking_its_source` leaked the raw plural source through the tag, and mutation M8 reddens it |
| ICU-4 | delegated | - | core `ICU-4` (implemented, n/a (pure)). Probe `interpolation` matches nothing here and fires on core `interpolate.py` |
| ICU-5 | delegated | - | core `ICU-5` (implemented, n/a (pure)). Probe `interpolation` matches nothing here and fires on core `interpolate.py` |
| ICU-6 | delegated | - | core `ICU-6` (implemented, n/a (pure)). Probe `interpolation` matches nothing here and fires on core `interpolate.py` |
| CID-1 | delegated | - | core `CID-1` (implemented, n/a (pure)). Probe `block-identity`: no id is derived here. It fires on core `client.py` |
| CID-2 | delegated | - | core `CID-2` (implemented, n/a (pure)). Probe `block-identity` matches nothing here and fires on core `client.py` |
| CID-3 | delegated | - | core `CID-3` (implemented, n/a (pure)). Probe `block-identity` fires on core `client.py`. This binding reads ids only through the core, which carries the tolerating half |
| CID-4 | delegated | - | core `CID-4` (implemented, n/a (pure)). Probe `block-identity` matches nothing here and fires on core `client.py` |
| TOK-1 | delegated | - | core `TOK-1` (implemented, n/a (pure)). Probe `tokenizer`: there is no tokenizer here. It fires on core `client.py`. Page and block translation are reached only as `get_client().translate_page(…)`, which is the core by reference |
| TOK-2 | delegated | - | core `TOK-2` (implemented, n/a (pure)). Probe `tokenizer` matches nothing here and fires on core `client.py` |
| TOK-3 | delegated | - | core `TOK-3` (implemented, n/a (pure)). Probe `tokenizer` matches nothing here and fires on core `client.py` |
| TOK-4 | delegated | - | core `TOK-4` (implemented, n/a (pure)). Probe `tokenizer` matches nothing here and fires on core `client.py` |
| TOK-5 | delegated | - | core `TOK-5` (implemented, n/a (pure)). Probe `interpolation` matches nothing here and fires on core `interpolate.py` |
| TOK-6 | delegated | - | core `TOK-6` (implemented, n/a (pure)). Probe `tokenizer` matches nothing here and fires on core `html/parser.py` |
| MARK-1 | delegated | - | core `MARK-1` (implemented, n/a (pure)). Probe `identity-stamping`: no host is stamped here. It fires on core `client.py` |
| MARK-2 | delegated | - | core `MARK-2` (implemented, n/a (pure)). Probe `identity-stamping` matches nothing here and fires on core `client.py` |
| MARK-3 | delegated | - | core `MARK-3` (implemented, n/a (pure)). Probe `identity-stamping` matches nothing here and fires on core `client.py` |
| MARK-4 | delegated | - | core `MARK-4` (implemented, n/a (pure)). Probe `identity-stamping` matches nothing here and fires on core `client.py` |
| SSR-1 | n/a (profile: browser) | - | Where the browser SDK collects under the client strategy. This is a server binding |
| SSR-2 | n/a (profile: browser) | - | The browser SDK degrading its strategy when a grant is configured. This is a server binding |
| SSR-3 | n/a (profile: browser) | - | The browser SDK's server-strategy precondition. This is a server binding |
| SRV-1 | implemented | n/a (pure) | `{% t %}`, the `t` filter and `t()` render the request locale's current translations into the served bytes. `test_SRV1_served_bytes_carry_the_request_locale` asserts on `response.content`; its control is a phrase absent from the catalog in the same render, which emits the base language and registers. `test_SRV1_a_streamed_render_carries_the_request_locale` covers the streamed path, which rendered the base language at `34a6a87`. Mutation M4 reddens it. These are the origin's bytes, and the `Vary` headers SRV-6 adds keep a shared cache from serving them to a visitor in another locale |
| SRV-2 | implemented | n/a (pure) | The request locale lives in a `ContextVar`, never a process global, and the core keys its catalog by locale. `test_SRV2_concurrent_requests_never_see_each_others_locale` pins the interleave with two barriers. Mutation M5, a process-global locale, reddens it and `test_middleware_resets_locale_after_request`. With a single barrier this test stayed green under M5 (item 8) |
| SRV-3 | implemented | n/a (pure) | The middleware opens one of the core's request scopes for every request and ends it when the response is closed, once the body, streamed or not, has gone out; `request_finished` then flushes. A miss recorded inside the scope is sent by no flush before that: not the core's debounce (`test_SRV3_the_core_debounce_never_sends_before_the_response_is_complete`), not a concurrent request's flush (`test_SRV3_another_requests_flush_never_sends_a_render_still_in_progress`), and not before a slow stream completes (`test_SRV3_a_streamed_body_holds_its_misses_until_it_is_complete`). `test_SRV3_registration_happens_only_after_the_response_is_complete` and `test_SRV3_a_streamed_body_is_complete_before_its_misses_are_sent` pin the order at the request boundary, and `test_SRV3_a_view_that_raises_still_releases_its_misses` covers a view that leaves no response to close. The read-only half is the core's decision at its send site, and the binding opens no send path around it (probes `capability` and `registration-request`); `test_SRV3_a_read_only_key_pushes_nothing`, with `test_SRV3_control_a_write_key_on_the_same_render_pushes` as its control, confirms it through the handler. Mutations M10 (ending the scope when the middleware returns), M11 (no scope), M12 (a scope never ended) and M13 (a view that raises leaves it open) each redden a named test |
| SRV-4 | n/a (architecture: Django templates emit terminal HTML and nothing hydrates against it, so there is no catalog to hand off; live if this binding ever ships a client entry that hydrates) | - | The spec's per-SDK profile table names this package terminal HTML, so SRV-4 is `n/a (architecture)` |
| SRV-5 | n/a (architecture: no tag here captures a rendered child subtree, since `{% t %}` takes its phrase as an argument; live if a block tag that captures its rendered body is added) | - | The once-per-subtree half belongs to a DOM-walking path. Here that is the core's `translate_page`, reached by reference only |
| SRV-6 | implemented | n/a (pure) | Where Django's `LocaleMiddleware` resolved the request's language, the middleware passes `request.LANGUAGE_CODE` to the core's `resolve_request_locale(framework=…)`, which maps it to the project's form (a bare `es` is authorization's default Spanish locale) and serves the base for a locale the project does not serve; the binding adds no `Vary`. Where nothing resolved it, the middleware passes the core its own candidates: the URL (the `QUERY_PARAM` query parameter, or the language prefix of `i18n_patterns`, counted only when the URLconf routes by one), the `COOKIE_NAME` cookie, and `Accept-Language`, serves the core's choice with the `Vary` headers it names, and writes no cookie. The order, the validation and the snapshot's served set while authorization is unavailable are the core's. `test_SRV6_the_locale_django_resolved_wins_and_the_sdk_adds_no_vary`, `test_SRV6_a_bare_language_from_django_is_the_projects_default_locale_for_it` and `test_SRV6_a_django_locale_the_project_does_not_serve_is_served_as_the_base`, with mutation M32; `test_SRV6_the_url_wins_and_needs_no_vary`, `test_SRV6_a_cookie_beats_the_header_and_varies_on_cookie`, `test_SRV6_the_header_decides_last_and_varies_on_it` and `test_SRV6_an_unsupported_cookie_falls_through_and_is_never_rewritten`, with mutations M14–M17 |
| MSG-1 | implemented | n/a (pure) | Each entry carries Django's or DRF's field path (`email`, or `items.1.name` through nested serializers and lists) and the framework's own `code`. The error body is the framework's: `error_response(form)` answers with Django's `form.errors.get_json_data()`, and `langsys_django.drf.exception_handler` leaves DRF's response as DRF wrote it; both attach the entries beside it through the core's `attach_server_messages`, under the `RESPONSE_KEY` setting or the core's default key. A DRF response that is not a failed validation is DRF's own. Building and filling the entry is the core's (probe `message-internals` matches nothing here and fires on core `messages.py`). `test_MSG1_a_failed_form_answers_with_djangos_body_and_the_entries_beside_it`, `test_MSG1_a_failed_request_keeps_drfs_body_with_the_entries_beside_it`, `test_MSG1_the_key_the_entries_sit_under_is_configurable`, `test_MSG1_a_response_that_is_not_a_failed_validation_is_drfs_own` and `test_MSG1_fields_are_dotted_paths_through_nested_serializers_and_lists`; mutation M28 (the entries replace DRF's body) reddens named tests |
| MSG-2 | implemented | n/a (pure) | An entry's `code` is Django's `ValidationError.code` or DRF's `ErrorDetail.code`, passed through; a Django failure raised with no code carries none. No code chooses text. `test_MSG2_each_failure_keeps_djangos_own_code_and_sentence`, `test_MSG9_a_text_only_failure_is_its_own_text_with_no_code_or_params`, `test_MSG2_an_apps_code_passes_through` and `test_MSG2_size_rules_keep_drfs_codes_and_bounds`; mutation M22 (every failure given one shared code) reddens named tests. Probe `shared-codes` finds no vocabulary of codes and no authoring-form wording here, and fires on a synthetic mapping |
| MSG-3 | implemented | n/a (pure) | A template is Django's or DRF's own sentence in the source language, read with Django's translation switched off: `This field is required.` as written. Django's `%(name)s` placeholders become `{name}` markers, and DRF's `{name}` placeholders are markers as they stand. Where the sentence names a field or a model (`%(field_label)s`, `%(field_labels)s`, `%(model_name)s`, `%(date_field_label)s`, DRF's `{field_names}`), the name the framework prints is written in. A plural message takes the form its count selects. `test_MSG3_a_sentence_that_names_no_field_is_one_template_as_django_wrote_it`, `test_MSG3_a_sentence_that_names_the_field_has_djangos_label_written_in`, `test_MSG3_a_value_that_is_not_an_object_names_its_type_in_a_marker` and `test_MSG3_a_choice_names_the_rejected_input_in_a_marker`; mutations M20, M21 and M26 redden named tests |
| MSG-4 | implemented | n/a (pure) | Params are the framework's own values: Django's `params`, and for DRF the field's bound or the rejected input, a number as a number. The core fills `message` and omits `params` for a template with no markers (probe `message-internals`, which fires on core `messages.py`). `test_MSG4_a_numeric_param_is_a_json_number` and `test_MSG3_a_sentence_that_names_no_field_is_one_template_as_django_wrote_it` |
| MSG-5 | implemented | n/a (pure) | `{% t_message entry %}` renders through the core's `render_server_message`: the template's translation in the request's locale, filled from its params, or `message` when the catalog has none. `form\|langsys_errors` hands a template a form's entries. `test_MSG5_an_entry_renders_its_translation_and_otherwise_its_message`, and `test_MSG5_the_filled_message_is_never_the_lookup_key`, whose catalog holds a decoy keyed by the filled message; mutation M24 (rendering by looking the message up) reddens it |
| MSG-6 | delegated | - | core `MSG-6` (implemented, contract). Probe `message-internals` matches nothing here and fires on core `messages.py`. The core's `message_category`, `Errors` by default, is the one category; the binding names none |
| MSG-7 | implemented | n/a (pure) | `langsys_django.messages.declared_templates(form_classes)` and `langsys_django.drf.declared_templates(serializer_classes)` list every template the app can emit: each field's `error_messages`, its validators' `message` (a plural one in the form its bound selects), a model's uniqueness messages as `Model.unique_error_message` builds them, and what a validator function or `clean`/`validate` method names with `@declares`. An undeclared validator is reported with a `PROBLEM` line and the fix. `python manage.py langsys_messages --provider …` runs the core's listing, registers through the app's client with `--register`, exits zero on reported problems, and non-zero only with `--strict`. `test_MSG7_the_provider_lists_djangos_own_sentences`, `test_MSG7_a_plural_message_lists_the_form_its_bound_selects`, `test_MSG7_an_undeclared_validator_is_a_problem_and_a_declared_one_is_listed`, `test_MSG7_a_model_forms_uniqueness_is_listed_with_djangos_labels`, `test_MSG7_the_command_reports_problems_and_fails_only_under_strict`, `test_MSG7_the_provider_lists_drfs_own_sentences` and `test_MSG7_a_model_serializers_uniqueness_is_listed_with_drfs_field_names`; mutation M30 (failing without `--strict`) reddens named tests |
| MSG-8 | delegated | - | core `MSG-8` (implemented, contract). Probe `message-internals` matches nothing here and fires on core `messages.py`. The core queues an unlisted template on its ordinary flush path, which inside a Django request runs within the request scope (SRV-3). Through a DRF request, `test_MSG1_a_failed_request_keeps_drfs_body_with_the_entries_beside_it` asserts that the failed request's templates are registered after its response |
| MSG-9 | implemented | n/a (pure) | Forms: each entry comes from the `ValidationError` Django raised, its unfilled `message` and its `params`. DRF: `ErrorDetail` holds rendered text, so the unfilled message is the field's own `error_messages[code]` or its validator's, filled from where DRF reads its values, and it is taken as DRF's only when DRF's rendering of it in the request's language is the reported text; a Django validator on a DRF field is re-run for its own message and params. A failure that is finished text only, a custom validator's sentence, is that text with no params. `test_MSG9_two_failed_rules_on_one_field_become_two_entries` (forms and DRF), `test_MSG9_a_text_only_failure_is_its_own_text_with_no_code_or_params`, `test_MSG9_an_apps_own_message_and_code_follow_djangos_convention`, `test_MSG9_a_form_wide_failure_carries_no_field`, `test_MSG9_a_validators_own_sentence_is_kept_as_written_with_drfs_code` and `test_MSG9_a_django_validator_on_a_drf_field_keeps_djangos_sentence`; mutations M21, M26, M27 and M29 redden named tests |
| MSG-10 | implemented | n/a (pure) | The label written in is the one the framework prints: a model field's `verbose_name`, and otherwise the name Django derives from its key. The listing names each validated field with no declared label (a form field `label`, `Meta.labels`, a DRF field's `label`, or a model field's `verbose_name`) on an `ADVICE` line that never fails the command. A bound DRF field always carries a label humanised from its key, so only a label actually given counts as declared. `test_MSG10_an_undeclared_label_is_advice_naming_the_field`, `test_MSG10_a_model_serializer_declares_with_verbose_name` and `test_MSG7_an_undeclared_validator_is_a_problem_and_an_unlabelled_field_advice`; mutations M23 and M25 redden named tests |
| MSG-11 | implemented | n/a (pure) | The listing command names this binding's label placeholders to the core's template list: Django's `%(field_label)s`, `%(field_labels)s`, `%(model_name)s` and `%(date_field_label)s`, and DRF's `{field_names}`. A template still holding one is refused with a `PROBLEM` line. The fill-time warning is the core's (probe `message-internals` matches nothing here and fires on core `messages.py`). `test_MSG11_a_template_holding_a_django_label_placeholder_is_refused` and `test_MSG11_a_template_holding_drfs_field_names_placeholder_is_refused`; mutation M31 (DRF's placeholder not passed) reddens named tests |
| MSG-12 | n/a (architecture: a failed Django form re-renders in the same response, so no entries cross a redirect to a client SDK; live if this binding ever adapts an Inertia-style hand-off) | - | There is no redirect hand-off to carry entries across |
| MIG-1 | delegated | - | core `MIG-1` (implemented, n/a (pure)). Probe `migration-files` matches nothing here and fires on a synthetic `polib` read. The binding reads no legacy translation file; resolving and converting one is the core's |
| MIG-2 | delegated | - | core `MIG-2` (implemented, n/a (pure)). Probe `migration-files` matches nothing here and fires on a synthetic `polib` read. The binding reads no legacy translation file; resolving and converting one is the core's |
| MIG-3 | delegated | - | core `MIG-3` (implemented, n/a (pure)). Probe `migration-files` matches nothing here and fires on a synthetic `polib` read. The binding reads no legacy translation file; resolving and converting one is the core's |
| MIG-4 | delegated | - | core `MIG-4` (implemented, n/a (pure)). Probe `migration-files` matches nothing here and fires on a synthetic `polib` read. The binding reads no legacy translation file; converting a legacy value is the core's |
| MIG-5 | delegated | - | core `MIG-5` (implemented, n/a (pure)). Probe `migration-files` matches nothing here and fires on a synthetic `polib` read. The binding reads no legacy translation file; resolving and converting one is the core's |
| MIG-6 | delegated | - | core `MIG-6` (implemented, n/a (pure)). Probe `migration-files` matches nothing here and fires on a synthetic `polib` read. The binding reads no legacy translation file; resolving and converting one is the core's |
| MIG-7 | delegated | - | core `MIG-7` (implemented, n/a (pure)). Probe `migration-files` matches nothing here and fires on a synthetic `polib` read. The binding reads no legacy translation file; resolving and converting one is the core's |
| MIG-8 | not implemented | - | Django's entry points (`gettext` and `_()`, `ngettext`, `pgettext`, `npgettext`, `{% translate %}` and `{% blocktranslate %}`) are not yet wired to the core's `translate_legacy(entry_point=…)` |
| MIG-9 | delegated | - | core `MIG-9` (not implemented, -). Probe `migration-files` matches nothing here and fires on a synthetic `polib` read. The binding reads no legacy translation file; the one-time import is the core's |
| SNAP-1 | delegated | - | core `SNAP-1` (implemented, contract). Probe `snapshot` matches nothing here and fires on a synthetic loader call. The export command is the core's |
| SNAP-2 | delegated | - | core `SNAP-2` (implemented, contract). The app seeds the core's catalog through `get_client().load_snapshot(…)`; this binding adds no seeding of its own. Probe `snapshot` matches nothing here and fires on a synthetic loader call |
| SNAP-3 | delegated | - | core `SNAP-3` (implemented, n/a (pure)). Probe `snapshot` matches nothing here and fires on a synthetic loader call |
| BIND-1 | partial | n/a (pure) | Only timing and shape are adapted: the HTTP request's locale goes into the core's `LocaleSource`, streamed chunks render in that locale, and each request runs inside one of the core's request scopes, and the core's public flush and reset run when Django ends it. With the binding deleted, calling the core directly changes only where the locale comes from and when those two public calls happen, except for the collision below. One guarantee this binding had narrowed was undefined template arguments reaching the core as `""` (item 6). That is fixed, and mutation M8 reddens `test_ICU2_an_undefined_template_variable_is_an_absent_argument` and `test_ICU3_a_plural_over_an_undefined_variable_recovers_instead_of_leaking_its_source`. The other adaptations are covered by M4, which reddens `test_SRV1_a_streamed_render_carries_the_request_locale`, and M5, which reddens `test_SRV2_concurrent_requests_never_see_each_others_locale`. **Still narrowed:** the `t(phrase, category=None, **params)` shape cannot pass a placeholder named `category` or `phrase`, which the core's `params` dict can (item 10). The silent misroute is pinned by `test_gap_a_category_keyword_is_taken_as_the_catalog_category` and `test_gap_the_tag_takes_a_category_keyword_as_the_catalog_category`. The loud forms are pinned by `test_gap_a_category_beside_a_category_keyword_raises`, `test_gap_in_a_template_the_collision_raises_while_rendering` and `test_gap_a_phrase_keyword_collides_too`. `test_workaround_the_core_takes_any_parameter_name` is the control. Waits on: one signature decision across the core, FastAPI and Django |
| BIND-2 | implemented | n/a (pure) | Probe `capability` matches nothing here and fires on `middleware.py` at `34a6a87`, where the defect itself lives. `test_BIND2_capability_unknown_holds_the_queue_through_the_binding` was red at `34a6a87`. Mutation M1, restoring the `can_write` branch in the request hook, reddens that test along with the `capability` and `queue-mutation` probes |
| BIND-3 | implemented | n/a (pure) | Probe `network-client` shows there is no HTTP client here and fires on core `http.py`. Probe `scheduling` shows there is no timer, sleep, retry or backoff and fires on core `client.py`. Opening and ending the core's request scope, and calling its public flush when Django ends a request, is lifecycle timing (BIND-1), not scheduling: the binding sets no timer and leaves the core's debounce as it is. Both probes run as `test_probe_matches_nothing_in_this_binding`, and their controls as `test_probe_fires_on_its_control`. The one header the binding sets is `Vary` on the response, which SRV-6 requires; it builds no request to the API |
| BIND-4 | implemented | n/a (pure) | The binding's settings are the core's constructor arguments (`API_KEY`, `PROJECT_ID`, `API_URL`, `BASE_LOCALE`) plus SRV-6 wiring, which BIND-4 allows: `QUERY_PARAM` names the query parameter that carries a URL locale, and `COOKIE_NAME` the cookie the app keeps one in. No setting changes product behaviour. `test_BIND4_no_binding_setting_switches_registration_off` shows an `AUTO_FLUSH` key has no effect, and probe `binding-discovery-switch` fires on `conf.py` at `34a6a87`, where it was a discovery switch. Mutation M6 restores that switch and reddens the test along with the `binding-discovery-switch` and `queue-mutation` probes |
| BIND-5 | implemented | n/a (pure) | Probe `cache` matches nothing here and fires on core `catalog.py`. No lookup is memoised here, so presence cannot be lost in this layer. The single shared client is an instance holder, not a lookup cache. The probe runs as `test_probe_matches_nothing_in_this_binding` |
| BIND-6 | implemented | n/a (pure) | `test_BIND6_public_names_are_django_idioms_over_core_values` pins `__all__` to six names and asserts that `t` mirrors the core's own alias. `test_BIND6_the_core_is_reachable_by_reference_not_through_a_wrapper` asserts that `get_client()` returns the core instance itself. The tag is a `SimpleNode` subclass, which is a Django idiom and not a new behaviour name |
| GRANT-1 | n/a (profile: browser) | - | A grant lends write capability to a browser session, and a server already holds a key. This binding sets no header of any kind (probe `auth-header`), so it never sends `X-Write-Grant` |
| GRANT-2 | n/a (profile: browser) | - | Resolving a browser grant per request. A server holds a key |
| GRANT-3 | n/a (profile: browser) | - | Re-authorizing on a new browser grant. A server holds a key |
| GRANT-4 | n/a (profile: browser) | - | The `X-Write-Grant` header. This binding sets no header |
| CACHE-1 | delegated | - | core `CACHE-1` (implemented, n/a (pure)). Probe `cache`: no cache key is built here. It fires on core `catalog.py` |
| CACHE-2 | delegated | - | core `CACHE-2` (implemented, contract). Probe `cache` matches nothing here and fires on core `catalog.py`. The binding caches no fetch, failed or otherwise |
| OBS-1 | delegated | - | core `OBS-1` (implemented, contract). Probe `diagnostics`: this binding logs nothing of its own. It fires on core `client.py` and on `middleware.py` at `34a6a87`. The request boundary is what re-arms the core's once-per-session notice: see `test_GATE3_the_unusable_capability_notice_rearms_at_each_request` |
| WIRE-1 | delegated | - | core `WIRE-1` (implemented, live). Probe `auth-header` matches nothing here and fires on core `http.py` |
| WIRE-2 | delegated | - | core `WIRE-2` (implemented, contract). Probe `response-parsing`: no API response is parsed here. It fires on core `http.py` |
| WIRE-3 | delegated | - | core `WIRE-3` (implemented, live). Probe `identifier-normalisation`: there is no lowercasing or sentinel here. It fires on core `catalog.py`. The middleware hands the core `canonicalize_locale` output, and the core lowercases it on the wire |
| WIRE-4 | delegated | - | core `WIRE-4` (implemented, live). Probe `raising`: nothing on this binding's paths raises. It fires on core `http.py`. The request hook calls `flush_pending()`, which the core guarantees never raises (REG-10) |
| WIRE-5 | implemented | n/a (pure) | The seam is `LANGSYS["API_URL"]`, documented in the README settings reference along with its ordering constraint. `test_WIRE5_API_URL_sends_the_client_to_the_double` proves a request arrives at the double. `test_WIRE5_a_redirect_after_the_client_is_built_is_too_late_until_reset` redirects too late and observes the request still reaching the old host until `reset_client()`, which is the failure the rule names. Mutation M9, dropping `API_URL` on its way to the core, reddens both |
| CONF-1 | implemented | n/a (pure) | No row here claims server acceptance. Every binding-owned row is an in-process property, and every API-dependent property is delegated with the core's tier named. Every-path clause: the binding's entry points are `{% t %}`, the `t` filter, `t()` and a streamed render. `test_GATE7_every_entry_point_reaches_the_core_queue`, `test_SRV1_a_streamed_render_carries_the_request_locale` and `test_SRV3_a_streamed_body_is_complete_before_its_misses_are_sent` prove each path rather than one |
| CONF-2 | implemented | n/a (pure) | Every row carries a tier from the checker vocabulary, and delegated rows take `-`. This is enforced by `test_every_status_and_tier_is_in_the_checker_vocabulary` and `test_a_delegated_row_names_its_core_row_and_a_probe_that_covers_it`. No row claims `contract`. No binding-owned row is graded on an absence at the double, so the drift CONF-2 requires has nothing to bind here: the one absence this file cites, a read-only key pushing nothing (SRV-3), is the core's decision and is cited as confirmation |
| CONF-3 | implemented | n/a (pure) | Every binding-owned runtime row names a mutation that reddens a named test. The mutations are M1–M32, applied by the committed `_dev_/mutations.py` to a copy of `src/` imported ahead of the package, against the green baseline M0; their results are in the table below. Every probe carries a firing control, and `test_probe_fires_on_its_control` fails when no control can run |

## Mutation evidence

Produced by `_dev_/mutations.py`; each line is from a run, not recalled. The conformance-file tests are
excluded from these runs because no mutation touches `CONFORMANCE.md`.

| Mutation | Suite | Reddens |
|---|---|---|
| M0 baseline, no edit | 141 passed | nothing |
| M1 restore the can_write branch in the request hook | 3 failed, 138 passed | `test_probes.py::test_probe_matches_nothing_in_this_binding[capability]`, `test_probes.py::test_probe_matches_nothing_in_this_binding[queue-mutation]`, `test_request_lifecycle.py::test_BIND2_capability_unknown_holds_the_queue_through_the_binding` |
| M2 no flush at the request boundary | 13 failed, 128 passed | `test_drf.py::test_MSG1_a_failed_request_keeps_drfs_body_with_the_entries_beside_it`, `test_request_lifecycle.py::test_BIND4_no_binding_setting_switches_registration_off`, `test_request_lifecycle.py::test_GATE3_an_observed_write_decision_does_not_survive_the_request`, `test_request_lifecycle.py::test_GATE3_the_unusable_capability_notice_rearms_at_each_request`, `test_request_lifecycle.py::test_GATE7_every_entry_point_reaches_the_core_queue`, `test_request_lifecycle.py::test_REG3_the_queue_is_registered_at_the_end_of_every_request`, `test_request_lifecycle.py::test_SRV1_served_bytes_carry_the_request_locale`, `test_request_lifecycle.py::test_SRV3_a_streamed_body_holds_its_misses_until_it_is_complete`, `test_request_lifecycle.py::test_SRV3_a_streamed_body_is_complete_before_its_misses_are_sent`, `test_request_lifecycle.py::test_SRV3_another_requests_flush_never_sends_a_render_still_in_progress`, `test_request_lifecycle.py::test_SRV3_control_a_write_key_on_the_same_render_pushes`, `test_request_lifecycle.py::test_SRV3_registration_happens_only_after_the_response_is_complete`, `test_request_lifecycle.py::test_SRV3_the_core_debounce_never_sends_before_the_response_is_complete` |
| M3 no reset at the request boundary | 2 failed, 139 passed | `test_request_lifecycle.py::test_GATE3_an_observed_write_decision_does_not_survive_the_request`, `test_request_lifecycle.py::test_GATE3_the_unusable_capability_notice_rearms_at_each_request` |
| M4 streamed body not rendered in the request locale | 2 failed, 139 passed | `test_request_lifecycle.py::test_SRV1_a_streamed_render_carries_the_request_locale`, `test_request_lifecycle.py::test_SRV3_a_streamed_body_is_complete_before_its_misses_are_sent` |
| M5 process-global locale instead of a ContextVar | 2 failed, 139 passed, 1 error | `test_request_lifecycle.py::test_SRV2_concurrent_requests_never_see_each_others_locale`, `test_wrapper.py::test_middleware_resets_locale_after_request`; errors: `test_resolved_marker.py::test_GATE10_outside_a_request_the_marker_prints_nothing` |
| M6 restore an AUTO_FLUSH switch | 3 failed, 138 passed | `test_probes.py::test_probe_matches_nothing_in_this_binding[binding-discovery-switch]`, `test_probes.py::test_probe_matches_nothing_in_this_binding[queue-mutation]`, `test_request_lifecycle.py::test_BIND4_no_binding_setting_switches_registration_off` |
| M7 the t filter bypasses the core | 2 failed, 139 passed | `test_request_lifecycle.py::test_GATE7_every_entry_point_reaches_the_core_queue`, `test_wrapper.py::test_template_tag_and_filter` |
| M8 the tag takes Django's empty string for a missing argument | 2 failed, 139 passed | `test_template_arguments.py::test_ICU2_an_undefined_template_variable_is_an_absent_argument`, `test_template_arguments.py::test_ICU3_a_plural_over_an_undefined_variable_recovers_instead_of_leaking_its_source` |
| M9 the settings API_URL never reaches the core | 2 failed, 139 passed, 2 errors | `test_settings_seam.py::test_WIRE5_API_URL_sends_the_client_to_the_double`, `test_settings_seam.py::test_WIRE5_a_redirect_after_the_client_is_built_is_too_late_until_reset`; errors: `test_settings_seam.py::test_WIRE5_API_URL_sends_the_client_to_the_double`, `test_settings_seam.py::test_WIRE5_a_redirect_after_the_client_is_built_is_too_late_until_reset`. The errors are at teardown: the requests went to the default API host, which no double serves |
| M10 end the request scope when the middleware returns | 1 failed, 140 passed | `test_request_lifecycle.py::test_SRV3_a_streamed_body_holds_its_misses_until_it_is_complete` |
| M11 no request scope: end it as soon as it opens | 3 failed, 138 passed | `test_request_lifecycle.py::test_SRV3_a_streamed_body_holds_its_misses_until_it_is_complete`, `test_request_lifecycle.py::test_SRV3_another_requests_flush_never_sends_a_render_still_in_progress`, `test_request_lifecycle.py::test_SRV3_the_core_debounce_never_sends_before_the_response_is_complete` |
| M12 never end the request scope | 13 failed, 128 passed | `test_drf.py::test_MSG1_a_failed_request_keeps_drfs_body_with_the_entries_beside_it`, `test_request_lifecycle.py::test_BIND4_no_binding_setting_switches_registration_off`, `test_request_lifecycle.py::test_GATE3_an_observed_write_decision_does_not_survive_the_request`, `test_request_lifecycle.py::test_GATE3_the_unusable_capability_notice_rearms_at_each_request`, `test_request_lifecycle.py::test_GATE7_every_entry_point_reaches_the_core_queue`, `test_request_lifecycle.py::test_REG3_the_queue_is_registered_at_the_end_of_every_request`, `test_request_lifecycle.py::test_SRV1_served_bytes_carry_the_request_locale`, `test_request_lifecycle.py::test_SRV3_a_streamed_body_holds_its_misses_until_it_is_complete`, `test_request_lifecycle.py::test_SRV3_a_streamed_body_is_complete_before_its_misses_are_sent`, `test_request_lifecycle.py::test_SRV3_another_requests_flush_never_sends_a_render_still_in_progress`, `test_request_lifecycle.py::test_SRV3_control_a_write_key_on_the_same_render_pushes`, `test_request_lifecycle.py::test_SRV3_registration_happens_only_after_the_response_is_complete`, `test_request_lifecycle.py::test_SRV3_the_core_debounce_never_sends_before_the_response_is_complete` |
| M13 a view that raises leaves its scope open | 1 failed, 140 passed | `test_request_lifecycle.py::test_SRV3_a_view_that_raises_still_releases_its_misses` |
| M14 the response carries no Vary | 4 failed, 137 passed | `test_request_locale.py::test_SRV6_a_cookie_beats_the_header_and_varies_on_cookie`, `test_request_locale.py::test_SRV6_an_unsupported_cookie_falls_through_and_is_never_rewritten`, `test_request_locale.py::test_SRV6_the_header_decides_last_and_varies_on_it`, `test_request_locale.py::test_SRV6_with_no_usable_candidate_the_base_locale_is_served` |
| M15 the cookie outranks the URL | 2 failed, 139 passed | `test_request_locale.py::test_SRV6_a_cookie_beats_the_header_and_varies_on_cookie`, `test_request_locale.py::test_SRV6_the_url_wins_and_needs_no_vary` |
| M16 the chosen locale is written back to a cookie | 2 failed, 139 passed | `test_request_locale.py::test_SRV6_an_unsupported_cookie_falls_through_and_is_never_rewritten`, `test_request_locale.py::test_SRV6_no_resolution_writes_a_cookie` |
| M17 any path prefix counts as a locale | 1 failed, 140 passed | `test_request_locale.py::test_control_a_path_that_starts_like_a_locale_is_not_one_without_i18n_patterns` |
| M18 a base-locale render is marked resolved | 1 failed, 140 passed | `test_resolved_marker.py::test_GATE10_a_base_locale_render_stays_unmarked` |
| M19 no render is ever marked resolved | 1 failed, 140 passed | `test_resolved_marker.py::test_GATE10_a_render_in_a_non_base_locale_marks_its_root` |
| M20 a label placeholder is left as a marker | 2 failed, 139 passed | `test_messages.py::test_MSG3_a_sentence_that_names_the_field_has_djangos_label_written_in`, `test_messages.py::test_MSG7_a_model_forms_uniqueness_is_listed_with_djangos_labels` |
| M21 the template is Django's rendered message | 6 failed, 135 passed | `test_drf.py::test_MSG9_a_django_validator_on_a_drf_field_keeps_djangos_sentence`, `test_messages.py::test_MSG2_each_failure_keeps_djangos_own_code_and_sentence`, `test_messages.py::test_MSG4_a_numeric_param_is_a_json_number`, `test_messages.py::test_MSG5_the_filled_message_is_never_the_lookup_key`, `test_messages.py::test_MSG9_an_apps_own_message_and_code_follow_djangos_convention`, `test_messages.py::test_MSG9_two_failed_rules_on_one_field_become_two_entries` |
| M22 every failure gets one shared code | 6 failed, 135 passed | `test_messages.py::test_MSG1_a_failed_form_answers_with_djangos_body_and_the_entries_beside_it`, `test_messages.py::test_MSG2_each_failure_keeps_djangos_own_code_and_sentence`, `test_messages.py::test_MSG9_a_form_wide_failure_carries_no_field`, `test_messages.py::test_MSG9_a_text_only_failure_is_its_own_text_with_no_code_or_params`, `test_messages.py::test_MSG9_an_apps_own_message_and_code_follow_djangos_convention`, `test_messages.py::test_MSG9_two_failed_rules_on_one_field_become_two_entries` |
| M23 an unlabelled field is not advised | 2 failed, 139 passed | `test_messages.py::test_MSG10_an_undeclared_label_is_advice_naming_the_field`, `test_messages.py::test_MSG7_the_command_reports_problems_and_fails_only_under_strict` |
| M24 an entry is rendered by looking its message up | 2 failed, 139 passed | `test_messages.py::test_MSG5_the_filled_message_is_never_the_lookup_key`, `test_probes.py::test_probe_matches_nothing_in_this_binding[message-internals]` |
| M25 a DRF label is the bound field's humanised one | 2 failed, 139 passed | `test_drf.py::test_MSG10_a_model_serializer_declares_with_verbose_name`, `test_drf.py::test_MSG7_an_undeclared_validator_is_a_problem_and_an_unlabelled_field_advice` |
| M26 a DRF template is DRF's rendered text | 5 failed, 136 passed | `test_drf.py::test_MSG1_fields_are_dotted_paths_through_nested_serializers_and_lists`, `test_drf.py::test_MSG2_size_rules_keep_drfs_codes_and_bounds`, `test_drf.py::test_MSG3_a_choice_names_the_rejected_input_in_a_marker`, `test_drf.py::test_MSG3_a_value_that_is_not_an_object_names_its_type_in_a_marker`, `test_drf.py::test_MSG9_two_failed_rules_on_one_field_become_two_entries` |
| M27 a DRF message is taken as DRF's own without checking its rendering | 1 failed, 140 passed | `test_drf.py::test_MSG9_a_validators_own_sentence_is_kept_as_written_with_drfs_code` |
| M28 the entries replace DRF's body | 2 failed, 139 passed | `test_drf.py::test_MSG1_a_failed_request_keeps_drfs_body_with_the_entries_beside_it`, `test_drf.py::test_MSG1_the_key_the_entries_sit_under_is_configurable` |
| M29 a Django validator on a DRF field is read as text | 1 failed, 140 passed | `test_drf.py::test_MSG9_a_django_validator_on_a_drf_field_keeps_djangos_sentence` |
| M30 the listing fails without --strict | 3 failed, 138 passed | `test_drf.py::test_MSG11_a_template_holding_drfs_field_names_placeholder_is_refused`, `test_messages.py::test_MSG11_a_template_holding_a_django_label_placeholder_is_refused`, `test_messages.py::test_MSG7_the_command_reports_problems_and_fails_only_under_strict` |
| M31 the listing is not told DRF's label placeholder | 1 failed, 140 passed | `test_drf.py::test_MSG11_a_template_holding_drfs_field_names_placeholder_is_refused` |
| M32 the locale Django resolved is ignored | 3 failed, 138 passed | `test_request_locale.py::test_SRV6_a_bare_language_from_django_is_the_projects_default_locale_for_it`, `test_request_locale.py::test_SRV6_a_django_locale_the_project_does_not_serve_is_served_as_the_base`, `test_request_locale.py::test_SRV6_the_locale_django_resolved_wins_and_the_sdk_adds_no_vary` |

## Absence probes

`tests/test_probes.py` holds 26 probes. Each is a pattern over this binding's code, with comments and
docstrings stripped by `ast`, and must match nothing. Each also has a control it must fire on: the core,
this binding at `34a6a87`, the TypeScript core, or a synthetic line that implements the behaviour.
A control that is unavailable on a machine is skipped, but a probe with no runnable control fails. The
probes read the package as imported, so a mutated copy is what they probe.

## Gaps, ranked by cost

1. **BIND-1: a placeholder named `category` or `phrase` cannot pass through `t()` or `{% t %}`.**
   `{% t "Browse {category}" category=cat.name %}` serves the placeholder unfilled. Python callers can pass
   the value through `get_client().translate(…, params={…})`; the fix is a `params` mapping like the core's,
   and the signature change is the operator's call.
2. **MIG-8: Django's gettext entry points are not wired to the core's `translate_legacy`.**
3. **SNAP-2: this binding does not seed a snapshot at startup.** The app can seed through
   `get_client().load_snapshot(…)`; a setting naming the snapshot file would do it at boot.
4. **Delegated rows the core has not made green.** 1 are not green and 0 are
   provisional, all core-side (see the summary).
